spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Suvam-Debnath/SpringCloudConfigServer-GitRepository
server:
  port: 8888

